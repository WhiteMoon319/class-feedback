# 待办与后续计划

## 计划中：每周汇总周报（LLM 辅助）

**状态**：设想已记录，待定三个决策点后开工（2026-09-09 记）。

### 设想内容

每周生成一份班级周报，四栏：

1. 本周反馈统计（共多少条、处理了多少）——纯 SQL 聚合，无需 LLM
2. 本周反馈主题转写——**需 LLM**，有隐私冲突（见下）
3. 班费收支——已有 `/api/finance/summary`
4. 本周公告——直接取

### 必须先解决的冲突

平台关于页承诺「数据仅存于本班 Cloudflare」。第二栏要把反馈内容发给第三方 LLM = **数据出域**，且反馈正文可能带身份线索（「我周三请假」「我们宿舍那层」）。做之前必须：

- 更新关于页，明确告知「周报由 AI 辅助生成，选中的反馈会发送至 XX 服务商」
- 第二栏采用**班委勾选制**：AI 只列候选（标题+笔名），班委勾选并可在发布前编辑措辞——伦理判断留在人手上，避免「匿名却被公开转写调侃」
- 备选：只发标题不发正文；或最保守方案——人工写摘要，不用 LLM

### 待定决策点

1. **LLM 服务商**：DeepSeek（国内直连、便宜）还是其他？（倾向 DeepSeek）
2. **周报范围**：全班公开，还是班委内部参考？（倾向全班公开）
3. **第二栏形态**：AI 自动转写 / AI 列候选+班委勾选（倾向此）/ 人工摘要

### 技术要点

- LLM 调用与密钥管理**复用博客**（`D:\program\blog` 的 `src/lib/ai.ts` + `ai-credentials.ts`），不另起炉灶
- 新增 `weekly_reports` 表：period、内容 JSON、生成时间、状态（草稿/已发布）、编辑人
- **手动触发优先**：管理端「生成周报草稿」按钮；cron 周任务只做「提醒班委该写周报」，避免无人审核时自动发布
- 展示：`/weekly` 页 + 首页入口
- 成本：每周一次调用、几千 token，可忽略

---

## 暂不做的 P3 项

规模不足以支撑，记录备查：

- 反馈广场 / 班务公开的搜索（当前分页足够，500+ 条后再考虑）
- 数据导出（班费 CSV / 审计日志导出）
- 举报处理结果回执（隐私权衡：回执可能暴露处理人视角）
- 附件孤儿对象清理任务（极端场景，R2 对象无引用时无害）
- PWA / 离线支持
