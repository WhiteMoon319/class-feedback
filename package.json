{
  "name": "class-feedback",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "license": "AGPL-3.0-or-later",
  "description": "大学班级匿名反馈与班务公开平台 · Cloudflare Workers + D1 + R2",
  "scripts": {
    "build": "node scripts/cf-config.mjs && node scripts/build-worker.mjs",
    "cf:config": "node scripts/cf-config.mjs",
    "cf:db:local": "node scripts/cf-config.mjs && wrangler d1 execute class-feedback-db --local --file=db/reset-local.sql && wrangler d1 migrations apply class-feedback-db --local && node scripts/dev-seed.mjs",
    "cf:dev": "npm run build && wrangler dev",
    "cf:deploy": "npm run build && wrangler deploy",
    "cf:secret": "wrangler secret put SESSION_SECRET",
    "test": "node --test --test-concurrency=1 \"tests/**/*.test.js\""
  },
  "devDependencies": {
    "esbuild": "^0.28.2",
    "wrangler": "^4.60.0"
  }
}
